
<h1><%= User.model_name.human %></h1>


<table>
  <thead>
    <tr>
      <th><%= User.human_attribute_name(:name) %></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
     <tr>
      <td><%= user.name %></td>
        <td><%= link_to t('views.common.show'), user_path(user.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @users %>

<%= link_to t('collections.books'), books_path %></td>



